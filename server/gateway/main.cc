#include <spdlog/spdlog.h>
#include <workflow/WFFacilities.h>

int main() {
    spdlog::info("Environment Check: spdlog is working!");
    spdlog::info("Environment Check: workflow is linked!");
    return 0;
}